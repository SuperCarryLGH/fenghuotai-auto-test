# ====================================================================
# 测试用户数据
# 所有 ID 用 KEY 命名，等你拿到真实数据后替换 VALUE 即可
# ====================================================================

users:
  normal_user:
    mobile: "15617617160"
    id: "USER_ID_NORMAL_001"       # TODO 替换为真实用户 ID
    remark: "普通用户，用于正常下单"

  whitelist_user:
    id: "USER_ID_WHITELIST_001"    # TODO 替换为真实用户 ID
    remark: "白名单用户，验证跳过风控"

  blacklist_user:
    id: "USER_ID_BLACKLIST_001"    # TODO 替换为真实用户 ID
    remark: "黑名单用户，验证直接禁止下单"

# ---------- 用户月下单次数（前置准备 / 后置校验）----------
user_month_counts:
  normal_user:    0
  whitelist_user: 0
  blacklist_user: 5
